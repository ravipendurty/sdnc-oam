name: Build, Deploy Maven Artifacts & Push Docker Images

on:
  workflow_dispatch:
  push:
    branches:
      - oslo-ios-mcn

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    env:
      GH_PAT: ${{ secrets.GH_PAT }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    permissions:
      contents: write
      packages: write  # Needed for both Maven deploy & GHCR push

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

      # - name: Configure GitHub Packages for Maven
      #   run: |
      #     mkdir -p ~/.m2
      #     echo "<settings><servers><server><id>github</id><username>${{ github.actor }}</username><password>${{ secrets.GITHUB_TOKEN }}</password></server></servers></settings>" > ~/.m2/settings.xml

      - name: Build and Deploy SDNC Artifacts
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: mvn -s ${GITHUB_WORKSPACE}/settings.xml --file ${GITHUB_WORKSPACE}/installation/sdnc/pom.xml deploy -DaltDeploymentRepository=github::default::https://maven.pkg.github.com/ravipendurty/sdnc-oam -DskipTests=true -Pdocker

      - name: Build and Deploy SDNC-WEB Artifacts
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: mvn -s ${GITHUB_WORKSPACE}/settings.xml --file ${GITHUB_WORKSPACE}/installation/sdnc-web/pom.xml deploy -DaltDeploymentRepository=github::default::https://maven.pkg.github.com/ravipendurty/sdnc-oam -DskipTests=true -Pdocker

      - name: Print Docker images
        run: docker images
        
      - name: Log in to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${GITHUB_REPOSITORY_OWNER} --password-stdin

      - name: Push Docker Images to GHCR
        env:
          OWNER: ${{ github.repository_owner }}
        run: |
          IMAGES=(
            onap/sdnc-image:3.0.3-SNAPSHOT-latest
            onap/sdnc-web-image:3.0.3-SNAPSHOT-latest 
          )
          for IMAGE in "${IMAGES[@]}"; do
            echo "$IMAGE"
            docker tag $IMAGE ghcr.io/$OWNER/$IMAGE
            docker push "ghcr.io/$OWNER/$IMAGE"
          done
